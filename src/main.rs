
fn main() -> Result<(), Box<dyn std::error::Error>> {
    println!("Singularity Web Engine v0.0.1");
    println!("Rebuilding the web from the ground up...");

    /*let url = url::parser::Url::parse("http://example.com:8080/path?query=value#fragment")?;
    println!("Parsed URL: {:?}", url);

    #[cfg(feature = "html")]
    {
        let html = "<html><body><h1>Chwała Freenetowi!</h1></body></html>";
        let document = html::parser::HtmlParser::parse(html)?;
        println!("Parsed HTML document with {} nodes", document.nodes.len());
    }*/

    Ok(())
}